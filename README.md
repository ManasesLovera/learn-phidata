# learn-phidata

Small learning project for building multi-agent workflows with `phidata`.

The current example wires together:
- a web research agent with DuckDuckGo
- a finance agent with Yahoo Finance tools
- a router agent that delegates between them using Gemini
